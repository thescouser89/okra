package org.jboss.pnc.okra.entity.okr;

import static org.junit.jupiter.api.Assertions.*;

class OkrTest {

    // an okr must have a non-empty / non-null name
    // an okr description can have > 255 chars
    // an okr must have an owner

}