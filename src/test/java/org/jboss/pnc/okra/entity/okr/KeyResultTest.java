package org.jboss.pnc.okra.entity.okr;

import static org.junit.jupiter.api.Assertions.*;

class KeyResultTest {
    // a key result  must have a non-empty / non-null name
    // a key result description can have > 255 chars
    // the key result unit must be set
    // the key result initial and final must not be the same
}