package org.jboss.pnc.okra.entity.team;

import static org.junit.jupiter.api.Assertions.*;

class TeamTest {

    // A team's name must be unique
    // A team's name must not be empty / null

    // A team's created and modified date must be set correctly

    // A team's description can be more than 255 chars
}