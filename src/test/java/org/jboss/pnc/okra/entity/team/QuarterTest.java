package org.jboss.pnc.okra.entity.team;

import static org.junit.jupiter.api.Assertions.*;

class QuarterTest {

    // For a quarter's year, it must not have duplicate names
    // a quarter's year and name must not be empty / null
    // Description can be more than 255 chars

    // A quarter created and modified date must be set correctly
    // A quarter's start and end period must be set
    // A quarter's start date must be *before* the end period

}